<%- include('../includes/head.ejs') %>

<body>
  <%- include('../includes/nav.ejs') %>
  <main>
    <% if (orders.length>0){%>
        
        <% orders.forEach(order=>{%>
                <h1># <%=order.id%></h1>
                    <ul>
                    <% order.products.forEach(item=>{%>
                        <li><%=item.title%>  (<%=item.orderItem.quantity%>)</li>
                            <% })%>
                    </ul>
    
        
            <%})%>
     <%}else{%>
    <h1>You haven't buy anythings...</h1>
    <%}%>
  </main>
  <%- include('../includes/end.ejs') %>

